# `/register`

Reduced from the design archive's 5-step KYC wizard: email, password, accept terms. The KYC steps
are out of v0 scope (`PROMPT.md` §4) — this route exists as a real, minimal signup, not a stub.
