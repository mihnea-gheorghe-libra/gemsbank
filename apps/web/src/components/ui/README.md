# `components/ui`

Primitives from the design archive's `industry.css` + `gems-theme.css`: `Button`, `Field`, `Plate`
(the archive's `.blueprint` frame, corner marks dropped per the GEMS theme), `Tag`, `Segmented`.

shadcn/ui + Radix underneath where a primitive earns it — only the components actually used
(`PROMPT.md` §2 dependency budget). Every visual value comes from `styles/tokens.css`; no
component in this folder may contain a hex value.
