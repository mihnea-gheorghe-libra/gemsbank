# `components/layout`

`SideNav` (the archive's numbered pill tabs — active state is plum fill, lime numeral),
`TopBar` (greeting, screen tag, theme + hide-balances toggles — Client Component), and
`SettingsSection` (kicker + rows, per archive "Settings" screen).
