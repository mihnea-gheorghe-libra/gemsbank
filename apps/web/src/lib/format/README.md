# `lib/format`

Dates, IBANs, locale helpers. **Not money** — that lives in `components/money/Money.tsx`. Two
money formatters means two rounding behaviours, and the second one is always written in a hurry.
