# `lib/api`

**Generated** client over `packages/contracts`. No hand-written request or response types — every
type crossing the network boundary comes from the OpenAPI spec FastAPI emits.

Regenerate with `make openapi`. CI fails if this drifts from the live spec.
