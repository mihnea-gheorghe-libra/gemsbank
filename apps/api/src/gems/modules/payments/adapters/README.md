# `payments/adapters`

Implementations of the ports declared in `payments/application`: database repositories, external
clients, stubs. This is where a port gets swapped for a real integration without touching
`application` or `domain`.
