[pytest]
testpaths = tests
pythonpath = ..
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    live_llm: calls the real Azure OpenAI deployment; needs credentials, Mongo, and network
addopts = -m "not live_llm"
