# `evals`

Offline evaluation for every `money-moving` path, including adversarial inputs (prompt injection
attempts against the amount, the beneficiary, the currency). An agent that can move money without
a regression suite covering these is not shippable — see the service README, "what must be true
before this is built".
